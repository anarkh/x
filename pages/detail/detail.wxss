.hero {
  position: relative;
  padding: 34rpx 32rpx;
  overflow: hidden;
  background:
    linear-gradient(135deg, rgba(229, 109, 63, 0.16), rgba(229, 109, 63, 0) 42%),
    linear-gradient(160deg, #17231f, #12372f);
  border-color: rgba(255, 255, 255, 0.08);
  box-shadow: 0 18rpx 52rpx rgba(23, 32, 29, 0.18);
}

.hero .title,
.hero .body {
  color: #f7fbf7;
}

.hero .muted {
  color: rgba(247, 251, 247, 0.68);
}

.hero-grid {
  position: absolute;
  right: -58rpx;
  top: -48rpx;
  width: 320rpx;
  height: 230rpx;
  border: 1rpx solid rgba(229, 109, 63, 0.2);
  background:
    linear-gradient(90deg, rgba(255, 255, 255, 0.06) 1rpx, transparent 1rpx),
    linear-gradient(0deg, rgba(255, 255, 255, 0.06) 1rpx, transparent 1rpx);
  background-size: 28rpx 28rpx;
  transform: rotate(-8deg);
}

.body {
  font-size: 30rpx;
  line-height: 1.58;
}

.detail-images {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12rpx;
}

.detail-images.single {
  grid-template-columns: minmax(0, 1fr);
}

.detail-image {
  width: 100%;
  height: 154rpx;
  border-radius: 8rpx;
  background: rgba(247, 251, 247, 0.12);
}

.detail-images.single .detail-image {
  height: 360rpx;
}

.hero-tags {
  flex-wrap: wrap;
  gap: 10rpx;
  min-width: 0;
}

.publish-success {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 20rpx;
  align-items: center;
  padding: 26rpx;
  border-color: rgba(229, 109, 63, 0.2);
  background: linear-gradient(135deg, #fff8f3, #ffffff);
}

.success-title {
  margin-bottom: 8rpx;
  color: #16231f;
  font-size: 30rpx;
  font-weight: 900;
  line-height: 1.25;
}

.success-actions {
  gap: 12rpx;
}

.share-button {
  width: 128rpx;
  max-width: 128rpx;
  height: 66rpx;
  padding: 0 24rpx;
  border-radius: 8rpx;
  background: linear-gradient(135deg, #e56d3f, #c84c30);
  color: #ffffff;
  font-size: 26rpx;
  font-weight: 900;
}

.mini-map {
  width: 100%;
  height: 340rpx;
  border-radius: 8rpx;
  overflow: hidden;
  border: 1rpx solid rgba(22, 49, 42, 0.1);
  box-shadow: 0 14rpx 36rpx rgba(23, 32, 29, 0.1);
}

.metrics {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  padding: 20rpx 0;
  background: rgba(255, 255, 255, 0.98);
  text-align: center;
}

.metric-item {
  position: relative;
}

.metric-item + .metric-item {
  border-left: 1rpx solid rgba(26, 46, 41, 0.1);
}

.metric {
  font-size: 38rpx;
  font-weight: 800;
  color: #1b5f52;
  line-height: 1.1;
}

.actions {
  display: grid;
  gap: 8rpx;
  align-items: stretch;
}

.actions.with-resolve {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.actions.without-resolve {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.actions button {
  width: 100%;
  min-width: 0;
  height: 60rpx;
  padding: 0 4rpx;
  border-radius: 8rpx;
  font-size: 21rpx;
  line-height: 60rpx;
  white-space: nowrap;
}

.resolve-button {
  background: linear-gradient(135deg, #e56d3f, #c84c30);
  box-shadow: 0 16rpx 34rpx rgba(167, 66, 40, 0.22);
}

.danger-button {
  width: 100%;
  height: 60rpx;
  border: 1rpx solid rgba(167, 53, 39, 0.14);
  border-radius: 8rpx;
  color: #a73527;
  background: linear-gradient(135deg, #ffe8e2, #fff3ef);
  font-weight: 900;
  font-size: 21rpx;
  line-height: 60rpx;
}

.disabled-action {
  opacity: 0.58;
  box-shadow: none;
}

.empty {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 24rpx;
}

.loading-mark {
  width: 54rpx;
  height: 54rpx;
  border: 6rpx solid rgba(31, 102, 88, 0.16);
  border-top-color: #1f6658;
  border-radius: 999rpx;
  animation: detail-spin 850ms linear infinite;
}

@keyframes detail-spin {
  from {
    transform: rotate(0deg);
  }

  to {
    transform: rotate(360deg);
  }
}
